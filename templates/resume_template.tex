\documentclass{resume}

\usepackage[left=0.4in, top=0.4in, right=0.4in, bottom=0.4in]{geometry}
\usepackage{hyperref}
\usepackage{fontawesome5}

\name{ {{ name }} }
\address{
    \href{tel:{{ phone_raw }}}{\faPhone\ {{ phone }}} \hspace{1em}
    \href{mailto:{{ email }}}{\faEnvelope\ {{ email }}} \hspace{1em}
    \href{ {{ linkedin_url }} }{\faLinkedin\ {{ linkedin }} } \hspace{1em}
    \href{ {{ github_url }} }{\faGithub\ GitHub } \hspace{1em}
    \href{ {{ leetcode_url }} }{LeetCode}
}


\begin{document}

    \begin{rSection}{Professional Summary}
    {{ summary }}
    \end{rSection}

    \begin{rSection}{Education}
    {% for edu in education %}
        \begin{rSubsection}{ {{ edu.institute }} }{ {{ edu.duration }} }{ {{ edu.degree }} }{ {{ edu.location }} }
            \item {{ edu.details }}
        \end{rSubsection}
            {% endfor %}
    \end{rSection}

    \begin{rSection}{Technical Skills}
        \begin{tabular}{ @{} >{\bfseries}l @{\hspace{4ex}} l }
            Languages    & {{ skills.languages | join(', ') }}    \\
            Frameworks   & {{ skills.frameworks | join(', ') }}   \\
            Technologies & {{ skills.technologies | join(', ') }} \\
            Databases    & {{ skills.databases | join(', ') }}    \\
            Tools        & {{ skills.tools | join(', ') }}        \\
        \end{tabular}
    \end{rSection}

    \begin{rSection}{Work Experience}
    {% for job in work_experience %}
        \begin{rSubsection}{ {{ job.company }} }{ {{ job.duration }} }{ {{ job.role }} }{ {{ job.location }} }
        {% for point in job.points %}
            \item {{ point }}
            {% endfor %}
        \end{rSubsection}
            {% endfor %}
    \end{rSection}

    \begin{rSection}{Projects}
    {% for project in projects %}
        \begin{rSubsection}{\href{ {{ project.url }} }{ {{ project.name }} }}{}{ {{ project.tech_stack }} }{}
        {% for point in project.points %}
            \item {{ point }}
            {% endfor %}
        \end{rSubsection}
            {% endfor %}

    \end{rSection}

    \begin{rSection}{Achievements}
        \begin{rSubsection}{}{}{}{}
        {% for item in achievements %}
            \item {{ item }}
            {% endfor %}
        \end{rSubsection}
    \end{rSection}

\end{document}
